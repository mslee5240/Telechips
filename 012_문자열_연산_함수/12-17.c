#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

int main() {
    char str1[80] = "pear";
    char str2[80] = "peach";

    char* line = strtok();
    if (strncmp(line, "AGE", 3) == 0) {
        // 나이를 처리하는 부분
    }
    else if (strncmp(line, "NAME", 4) == 0) {
        // 이름을 처리하는 부분
    }
    

    return 0;
}
