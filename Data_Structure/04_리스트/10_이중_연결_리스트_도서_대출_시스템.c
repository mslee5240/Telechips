#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "double_linked_list.h"

typedef DListNode Book;

// 도서 추가 함수
void add_book(DListNode* head, )

int main(void) {
    // 이중 연결 리스트 헤더 생성
    Book* head = (Book*)malloc(sizeof(Book));
    init(head);


    return 0;
}